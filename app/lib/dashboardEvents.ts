export type DashboardEventMap = {
  "hydration-updated": {
    score: number;
    color: string;
  };

  "activity-updated": {
    score: number;
    color: string;
  };

  "nutrition-updated": {
    score: number;
    color: string;
  };
};
